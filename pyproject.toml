[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hVsFigures"
version = "0.1.0"
description = "Code for reproducing figures in the paper"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]